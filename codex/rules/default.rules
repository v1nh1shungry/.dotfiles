prefix_rule(
    pattern=["git", ["add", "clean", "commit", "push", "rebase", "reset", "restore", "rm"]],
    decision="forbidden",
)

prefix_rule(
    pattern=["rm"],
    decision="forbidden",
)

# vim: ft=python:
